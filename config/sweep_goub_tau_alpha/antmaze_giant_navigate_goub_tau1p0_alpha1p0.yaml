# AntMaze giant navigate -- vanilla GOUB sweep over (spi_tau, subgoal_value_alpha).
# Generated by scripts/launch_goub_tau_alpha_sweep.sh.
# Hyperparameters mirror the large-navigate sweep; only train_epochs / actor.spi_tau /
# goub.subgoal_value_alpha differ. Omitted keys use main.py / agents/goub_dynamics defaults.

env_name: antmaze-giant-navigate-v0
seed: 0
run_group: antmaze_giant_navigate_goub_tau_alpha_sweep

train_epochs: 400
log_every_n_epochs: 10

batch_size: 1024
plan_candidates: 1
plan_noise_scale: 0.01

goub:
  subgoal_value_alpha: 1.0

critic_agent:
  action_chunk_horizon: 5
  discount: 0.99

actor:
  spi_tau: 1.0
  spi_beta: 1.0
  spi_actor_layer_norm: true
  spi_conditioned: subgoal
